z-indexes = {
    base: 1,
    jumbotron: 10,
    jumbotron-overlay: 20,
    jumbotron-content: 30,
    navigation: 90,
    fullscreen: 100,
    fullscreen-overlay: 110,
    fullscreen-modal: 120,
    alerts: 130
}

get-z-index(index)
    z-indexes[index]
